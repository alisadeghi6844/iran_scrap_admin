# سیستم مدیریت ایران‌اسکرپ

یک سیستم کامل مدیریت نظرسنجی با امکانات پیشرفته برای ایجاد، ویرایش، مدیریت و تحلیل نظرسنجی‌ها در پلتفرم ایران‌اسکرپ.

## ویژگی‌های اصلی

### 🎯 مدیریت نظرسنجی‌ها
- ایجاد نظرسنجی‌های جدید با سوالات متنوع
- ویرایش و به‌روزرسانی نظرسنجی‌های موجود
- حذف نظرسنجی‌ها
- تنظیم وضعیت انتشار (منتشر شده / پیش‌نویس)

### 📝 انواع سوالات پشتیبانی شده
- **انتخاب تکی (Single Choice)**: رادیو باتن‌ها
- **انتخاب چندگانه (Multiple Choice)**: چک باکس‌ها
- **متن (Text)**: پاسخ‌های متنی
- **عدد (Number)**: پاسخ‌های عددی
- **تاریخ (Date)**: انتخاب تاریخ

### 🔧 مدیریت سوالات
- افزودن سوالات جدید به نظرسنجی
- ویرایش سوالات موجود
- حذف سوالات
- تنظیم ترتیب سوالات
- تعیین اجباری بودن سوالات
- مدیریت گزینه‌ها برای سوالات انتخابی

### 📊 آمار و گزارش‌گیری
- نمایش آمار کلی نظرسنجی‌ها
- مشاهده پاسخ‌های دریافتی
- تحلیل نرخ تکمیل
- امکان خروجی گرفتن از پاسخ‌ها

### 👥 تجربه کاربری
- رابط کاربری مدرن و ریسپانسیو
- پیش‌نمایش نظرسنجی قبل از انتشار
- فرم تکمیل نظرسنجی برای کاربران عادی
- نوار پیشرفت در حین تکمیل نظرسنجی

## ساختار فایل‌ها

```
src/
├── components/survey/
│   ├── SurveyForm.tsx          # فرم ایجاد/ویرایش نظرسنجی
│   ├── SurveyList.tsx          # لیست نظرسنجی‌ها
│   ├── SurveyViewer.tsx        # پیش‌نمایش نظرسنجی
│   ├── SurveyResponses.tsx     # مشاهده پاسخ‌ها
│   ├── SurveyStats.tsx         # آمار نظرسنجی‌ها
│   ├── QuestionManager.tsx     # مدیریت سوالات
│   ├── SurveyTaker.tsx         # فرم تکمیل نظرسنجی
│   └── index.ts                # صادرات کامپوننت‌ها
├── page/surveyManagement/
│   ├── index.tsx               # صفحه اصلی مدیریت
│   ├── SurveyDetail.tsx        # جزئیات نظرسنجی
│   └── PublicSurvey.tsx        # صفحه عمومی نظرسنجی
├── redux/
│   ├── actions/survey/
│   │   └── SurveyActions.ts    # اکشن‌های Redux
│   ├── slice/survey/
│   │   └── SurveySlice.ts      # Slice مدیریت state
│   ├── service/survey/
│   │   └── SurveyServices.ts   # سرویس‌های API
│   ├── api/survey/
│   │   └── SurveyApi.ts        # آدرس‌های API
│   └── types/survey/
│       └── SurveyTypes.ts      # تایپ‌ها و اینترفیس‌ها
```

## API Endpoints

### نظرسنجی‌ها
- `POST /api/survey-admin` - ایجاد نظرسنجی جدید
- `GET /api/survey-admin?page=1&limit=10` - دریافت لیست نظرسنجی‌ها
- `GET /api/survey-admin/{id}` - دریافت جزئیات نظرسنجی
- `PATCH /api/survey-admin/{id}` - ویرایش نظرسنجی
- `DELETE /api/survey-admin/{id}` - حذف نظرسنجی

### سوالات
- `POST /api/survey-admin/{surveyId}/question` - افزودن سوال جدید
- `PATCH /api/survey-admin/question/{questionId}` - ویرایش سوال
- `DELETE /api/survey-admin/{surveyId}/question/{questionId}` - حذف سوال

### پاسخ‌های کاربران
- `GET /api/survey-admin/user-survey/{id}` - دریافت پاسخ خاص
- `GET /api/survey-admin/user-survey?surveyId={id}` - دریافت پاسخ‌های نظرسنجی

## نحوه استفاده

### 1. ایجاد نظرسنجی جدید
```typescript
const surveyData = {
  title: "نظرسنجی رضایت مندی",
  description: "لطفا نظر خود را درباره خدمات ما بیان کنید",
  published: true,
  questions: [
    {
      order: 0,
      text: "چقدر از خدمات ما راضی هستید؟",
      type: "SingleChoice",
      required: true,
      choices: ["بسیار راضی", "راضی", "متوسط", "ناراضی"]
    }
  ]
};

dispatch(CreateSurveyAction(surveyData));
```

### 2. مدیریت سوالات
```typescript
// افزودن سوال جدید
const questionData = {
  order: 1,
  text: "پیشنهادات شما چیست؟",
  type: "Text",
  required: false
};

dispatch(CreateQuestionAction({ surveyId: "1", data: questionData }));
```

### 3. دریافت پاسخ‌ها
```typescript
// دریافت پاسخ‌های یک نظرسنجی
dispatch(GetUserSurveysAction("surveyId"));
```

## کامپوننت‌های اصلی

### SurveyForm
فرم کامل برای ایجاد و ویرایش نظرسنجی‌ها با قابلیت:
- مدیریت اطلاعات پایه نظرسنجی
- افزودن و ویرایش سوالات
- تنظیم گزینه‌ها برای سوالات انتخابی
- اعتبارسنجی فرم

### SurveyList
نمایش لیست نظرسنجی‌ها با امکانات:
- مشاهده اطلاعات خلاصه
- دکمه‌های عملیات (ویرایش، حذف، مشاهده)
- نمایش وضعیت انتشار
- لینک به مشاهده پاسخ‌ها

### SurveyTaker
رابط تکمیل نظرسنجی برای کاربران با:
- نمایش سوالات به صورت مرحله‌ای
- نوار پیشرفت
- اعتبارسنجی پاسخ‌ها
- امکان بازگشت به سوالات قبلی

### QuestionManager
مدیریت پیشرفته سوالات شامل:
- افزودن سوالات جدید
- ویرایش سوالات موجود
- مدیریت گزینه‌ها
- تنظیم ترتیب سوالات

## تنظیمات Redux

سیستم از Redux Toolkit استفاده می‌کند و شامل:

### State Management
- مدیریت وضعیت لودینگ برای تمام عملیات
- مدیریت خطاها
- کش کردن داده‌ها
- به‌روزرسانی خودکار لیست‌ها

### Actions
- اکشن‌های async برای تمام عملیات API
- مدیریت خطاها با toast notifications
- بازگشت مقادیر مناسب برای UI

## استایل‌دهی

سیستم از Tailwind CSS استفاده می‌کند و شامل:
- طراحی ریسپانسیو
- رنگ‌بندی سازگار
- انیمیشن‌های نرم
- آیکون‌های React Icons

## ویژگی‌های امنیتی

- اعتبارسنجی سمت کلاینت و سرور
- محدودیت دسترسی به نظرسنجی‌های منتشر نشده
- مدیریت خطاها
- پاکسازی داده‌های ورودی

## توسعه آینده

امکانات پیشنهادی برای نسخه‌های بعدی:
- تحلیل پیشرفته آماری
- قالب‌های آماده نظرسنجی
- ارسال دعوتنامه ایمیلی
- گزارش‌گیری پیشرفته
- یکپارچه‌سازی با سیستم‌های خارجی
- پشتیبانی از فایل‌های چندرسانه‌ای در سوالات

## نصب و راه‌اندازی

1. کامپوننت‌ها و فایل‌های ایجاد شده را در پروژه قرار دهید
2. مسیرهای مربوطه را به routing اضافه کنید
3. API endpoints را در بک‌اند پیاده‌سازی کنید
4. تست کامل سیستم را انجام دهید

سیستم آماده استفاده است و می‌تواند به راحتی گسترش یابد.